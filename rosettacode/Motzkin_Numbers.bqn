# M(n + 1) = M(n) + sum(M(k) * M(n - k - 1) for k in [0..n-1])
M ← {
  0: 1;
  1: 1;
  (𝕊 𝕩-1) + +´ (⊢ ×○𝕊 (𝕩-2)-⊢)¨ ↕𝕩-1
}

#M¨ ↕42

IsPrime ← (2=·+´0=(1+↕)⊸|)

M2 ← {
  𝕩↑{
    n ← ¯1 + ≠𝕩
    𝕩 ∾ (n⊑𝕩) + (+´⊢×⌽) ¯1↓𝕩
  }⍟(0⌈𝕩-2) 1‿1
}

•Show >(↕∘≠∾¨⊢)(⊢∾IsPrime)¨ M2 12
